using UnityEngine;

namespace DeathLine {
    public enum RoleType { DPS, Tank, Support }
    public enum ColourAffinity { Neutral, Blue, Red, Yellow }
    public enum TargetType { Self, SingleAlly, SingleEnemy, AllAllies, AllEnemies, Row, Column, AoE }
}
